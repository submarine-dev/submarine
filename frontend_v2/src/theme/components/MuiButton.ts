import { ComponentsVariants, Theme } from '@mui/material';

/**
 * theme/componentsFn/MuiButton
 */
const MuiButton: { variants?: ComponentsVariants<Theme>['MuiButton'] } = {
  variants: [],
};

export default MuiButton;
